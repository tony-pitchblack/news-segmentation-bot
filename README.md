# News segmentation bot
To launch service use one of:
- `faster-whisper-client.py`
- `whisper-cpp-client.py`

## Model weights
A copy of model weights is available at `artifacts_saved/`
By default scripts use model ckpt downloaded to `artifacts/` from WandB *(need refactor)*